class StudentAttendance {
  final String name;
  final String email;
  final bool isPresent;

  StudentAttendance({
    required this.name,
    required this.email,
    required this.isPresent,
  });
}